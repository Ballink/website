---
interface Props {
  name: string;
  contactEmail: string;
}

const {name, contactEmail} = Astro.props
const currentYear = 2024
---

<footer
    class="mt-12 max-w-screen-xl mx-auto p-4 sm:p-6 lg:p-8 flex items-center justify-between"
>
  <span>{name}™ {currentYear}</span>

  <nav class="flex items-center gap-8">
    <a href="/about">About</a>
    <a href={`mailto:${contactEmail}`}>Contact</a>
  </nav>
</footer>
